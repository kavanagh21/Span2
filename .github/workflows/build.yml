name: Build SpAN

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.12"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyQt6 numpy pyinstaller

      - name: Build with PyInstaller
        run: |
          pyinstaller --onefile --windowed --name SpAN --runtime-hook hooks/hook-pyqt6.py --collect-all PyQt6 span/main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SpAN-Windows
          path: dist/SpAN.exe

  build-macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyQt6 numpy pyinstaller

      - name: Build .app bundle
        run: |
          pyinstaller \
            --windowed \
            --name SpAN \
            --osx-bundle-identifier com.deankavanagh.span \
            --runtime-hook hooks/hook-pyqt6.py \
            --collect-all PyQt6 \
            span/main.py

      - name: Patch Info.plist
        run: |
          plutil -replace CFBundleVersion -string "2.0.0" dist/SpAN.app/Contents/Info.plist
          plutil -replace CFBundleShortVersionString -string "2.0.0" dist/SpAN.app/Contents/Info.plist
          plutil -replace CFBundlePackageType -string "APPL" dist/SpAN.app/Contents/Info.plist

      - name: Zip .app to preserve symlinks
        run: |
          cd dist
          zip -r -y SpAN-macOS.zip SpAN.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SpAN-macOS
          path: dist/SpAN-macOS.zip

  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: SpAN-Windows
          path: artifacts/

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: SpAN-macOS
          path: artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/SpAN.exe
            artifacts/SpAN-macOS.zip
          generate_release_notes: true
